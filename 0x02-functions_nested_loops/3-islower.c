#include "main.h"

/**
 * _islower -> checks for all lowercase charactors
 * @c: charactor argument
 *Return: 1 and 0 depending on conditions
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
